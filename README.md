# Arch
A Arch install script
```
pacman -S git
fdisk -l
cfdisk /dev/sdX
mount /dev/sdXX /mnt
git clone https://github.com/Ganesha2282882/Arch.git iarch
cd iarch
chmod +x install.sh
./install.sh
```
Login: arch

Password: arch
